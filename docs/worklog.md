# 작업 기록

## 2026-07-14

1. `붙임1.txt`, `붙임2.txt`를 기본으로 공모분야, 제출조건, 평가기준, 선행사업·수상작을 파악했다.
2. 원본 PDF/HWP에서 표·서식·이미지처럼 텍스트만으로 불확실한 부분을 대조했다.
3. 지정공모 3의 대전 트램 공사 아이디어로 방향을 고정했다.
4. AI를 30분 교통예측 후보와 생성형 분석보조로 분리했다.
5. 사용자의 지적을 반영해 “권장 출차시간”을 제거했다.
6. 트램 공사 이후에는 개통전환·사후평가·다른 계획공사 재사용으로 연결하되, 다음 운영주체가 없으면 종료한다고 정했다.
7. 상용 TMS와 택배 코어시스템을 대체하지 않고 계획공사 위험정보를 제공하는 모듈로 수정했다.
8. `plan.md` v2와 초기 리서치·의사결정을 커밋하고 원격 `main`에 푸시했다.

## 2026-07-15 — 공모요건 재검증

1. NLIS 공식 게시물과 첨부파일을 확인하고 로컬 첨부 1·2의 해시 일치를 검증했다.
2. 이미지형 첨부 3·4를 페이지별로 직접 확인해 `붙임3.txt`, `붙임4.txt`로 전사했다.
3. 필수 5종 제출물, 제안서 10쪽, 분석보고서 3~5쪽 권장, 프롬프트 3개 이상, ZIP 50MB, 마감 8월 4일 14시를 잠갔다.

## 2026-07-15 — API·데이터 검증

1. 자격증명을 Windows 사용자 환경변수 `DATA_GO_KR_SERVICE_KEY`에만 저장하고 Git·문서·출력에서 제외했다.
2. 레거시 대전교통 API를 호출해 코드 30(`SERVICE KEY IS NOT REGISTERED ERROR`)을 확인했다.
3. 2026년 4월 등록된 최신 데이터셋 `15157924`와 새 게이트웨이를 발견해 다시 호출했으나 HTTP 403이었다.
4. 기상청 ASOS 시간자료를 대전 지점 133으로 호출했으나 HTTP 403이었다.
5. 대전 트램 공식 공구별 페이지 14개를 구조적으로 확인하고, 구체적 공사정보가 있는 1·12공구를 10분 수집 대상으로 정했다.
6. 국가교통 데이터 오픈마켓의 대전 15분·1시간·월별 상품 메타데이터를 확인했지만 실제 파일은 로그인이 필요해 미사용으로 판정했다.
7. ITS 공식 2024-11-29 표준 노드·링크 ZIP 125,973,128바이트를 내려받고 SHA-256을 검증했다. 대용량 원본은 Git에서 제외했다.
8. 최신 Swagger의 인증 변수명이 `serviceKey`, 응답 행이 `TRAFFIC`임을 확인해 구현과 회귀시험을 고쳤다. 수정 후 실제 호출도 HTTP 403 `Forbidden`이었고, 인증값을 빼면 HTTP 401이어서 자격증명 차단 판정은 유지했다.

## 2026-07-15 — 수집·분석 코드

1. Python 패키지, CLI, 환경설정, 데이터 모델을 만들었다.
2. 트램 페이지 수집기, 원응답 gzip·메타데이터 보존, SQLite 저장, 수집실행 로그를 구현했다.
3. 동일 라벨 중복을 임의 합치지 않고 발생순번 기반 안정 ID와 품질경고로 처리했다.
4. API·KMA 파서와 안전 호출 어댑터를 만들고 예외에 전체 요청 URL이 노출되지 않도록 했다.
5. 30분 뒤 예제를 과거 특성만으로 만드는 시간순 평가 파이프라인과 최소자료 게이트를 구현했다.
6. 지속성·동시간 기준모델과 히스토그램 그래디언트부스팅 후보 비교를 구현했다.
7. 현재 관측 위험 CSV/JSON과 샘플 계획경로 결합을 구현했다.
8. 로컬 대시보드와 상태·시작·중지 스크립트를 만들었다.

## 2026-07-15 — 공간매핑

1. 트램 공사 이벤트 5개를 수기 기준표로 만들고 출처·확인시각·상태를 기록했다.
2. 관측 라벨과 공사 이벤트의 포함/후보/제외, 신뢰도, 이유를 18행으로 기록했다.
3. 공식 표준 링크의 EPSG:5186 좌표와 방향·길이를 이용한 최단경로 생성기를 만들었다.
4. 서대전역네거리→서대전네거리는 4링크 654.016m, 역방향은 4링크 650.315m, 도로명 모두 계백로임을 재현했다.
5. 테미삼거리 인접도로가 충무로·테미로임을 확인해 계룡로 오매핑을 폐기했다.
6. 1공구 공식 공사이미지 2건의 URL·크기·PNG 규격·SHA-256과 공식 공지문 1건의 기대문구를 검증하는 근거 대장과 자동검사를 추가했다.
7. `tram-z01-01`, `tram-z01-02`의 공사범위는 high 신뢰로 확인하고 medium 이상 관측구간을 포함해 Gate 2를 통과했다.
8. `tram-z01-01`의 동일 라벨 발생순번은 좌표·공식 링크 ID가 없어 개별 매핑을 medium으로 유지했다. Gate 통과를 개별 위치 확정으로 해석하지 않는다.

## 2026-07-15 — 검증 결과

1. 자동 테스트 32개가 모두 통과했다.
2. 브라우저 자동검증으로 전체 41행, 1공구 9행, 12공구 32행, 이벤트 5개를 확인했다.
3. 브라우저 콘솔 오류와 페이지 오류는 0건이었다.
4. 01:41 점검 기준 12스냅숏·492행·41구간, 실패 0, 비정상/0 속도 0건이었다.
5. 실데이터가 48시간·3일·5,000예제 기준에 미달해 모델평가를 중단하고 예측필드를 null로 유지했다.
6. 최신 대전교통·KMA는 인증실패, 트램 공식 페이지와 표준 링크는 사용 가능으로 기계 판독 보고서에 기록했다.
7. 매핑 근거 실시간 검증은 공식 URL 3건과 이미지 해시 2건 모두 성공했고 Gate 2 상태는 `passed`였다.

## 2026-07-15 — 상가 기반 배송노출 검증

1. 공공데이터포털의 `소상공인시장진흥공단_상가(상권)정보_20260331` 전국 ZIP 341,021,001바이트를 실제 내려받아 SHA-256을 고정했다.
2. ZIP에서 대전 CSV 78,607행을 추출했고 좌표 유효 78,607건, 중복 상가 ID·타 시도·잘못된 좌표 0건을 확인했다.
3. 상가 WGS84 좌표를 EPSG:5186으로 변환하고 공사 이벤트의 공식 표준 링크/노드 250m 안의 점을 계산했다.
4. 계족로 69곳, 읍내삼거리 12곳, 계백로 994곳을 얻어 시범 관측구간 10개와 위험 CSV/JSON에 별도 필드로 연결했다.
5. 상가 수를 실제 택배 물량·주문량으로 해석하지 않도록 단위·기준일·공간/매핑 신뢰도와 경고를 출력했다.
6. 대시보드에 배송노출 열을 추가하되 현재 교통등급과 통합하지 않았다.

## 2026-07-15 — 안전한 최종화 자동화

1. 공식 데이터원·이미지·매핑 검증을 한 번에 갱신한 뒤 자료 게이트를 판정하는 `scripts/finalize_submission.ps1`를 만들었다.
2. 최소자료 미달이면 `pending`과 부족항목만 기록하고 동결 DB·모델·최종 매니페스트를 만들지 않도록 했다.
3. 통과 시 실행 중 DB의 SQLite 일관 복제본을 만들고 무결성 검사 후 시간순 모델평가, 현재 위험정보, 샘플 경로 결합을 재생성하도록 했다.
4. 동결 DB·모델·결과·근거 파일의 상대경로·크기·SHA-256을 최종 매니페스트에 기록한다.
5. 합성자료로 대기·비데이터 차단·성공 동결 세 경로를 검증했다. 실제 DB는 01:22 기준 10스냅숏·410행·1.337시간·164예제로 `pending`이며 동결 산출물 0건이다.
6. 최소자료만 충족한 상태를 대시보드가 ‘검증된 AI 예보’로 오표시할 수 있는 분기를 제거하고 ‘평가 준비·관측 전용’으로 고정했다.

## 2026-07-15 — 문서화

1. `plan.md`를 실제 검증결과가 반영된 v3로 갱신했다.
2. API 검증, 출처, 경쟁서비스, 데이터 계약, 의사결정, AI 활용 로그를 별도 문서로 작성했다.
3. FACT/RESULT/PLAN/ASSUMPTION/UNKNOWN을 분리하고 효과수치·도입·예측성능을 만들지 않았다.
4. 제안서 7쪽·분석 과정 보고서 3쪽 PDF를 재생성하고 A4·필수문구·빈 페이지·렌더링·로컬경로 노출을 검사했다.

## 현재 실행 상태

- 수집기: 1·12공구, 10분 주기, 백그라운드 실행 중
- 대시보드: `http://127.0.0.1:8765`, 백그라운드 실행 중
- 원자료·DB·대형 표준링크: 로컬에만 보존, Git 제외
- API 자격증명: 사용자 환경변수에만 보존, 값 미기록

## 다음 작업

1. 48시간 수집 후 자동 최종화로 모델 준비상태와 기준모델/AI 성능을 재평가한다.
2. 사용자 입력이 필요한 팀정보·동의·서명·심사위원 번호를 받는다.
3. HWP는 안전 편집 중단 사유에 따라 원본에서 수동 마감하고 PDF를 다시 검수한다.
4. 최신 공고·공사상태·출처·쪽수·ZIP 크기를 최종 감사한다.

## 2026-07-15 — 공식 제출조건 최종 감사

1. 접수 중인 PMS 공식 공고에서 2026-06-19 갱신 첨부 1~5의 메타데이터를 조회했다.
2. 첨부 1~4를 다시 내려받아 로컬 원본과 SHA-256을 대조했고 모두 일치했다.
3. 서식 1의 약어 지양 규칙에 따라 공식 주제를 `대전 트램 공사구간의 도심배송 위험 관측·예보 모듈`로 통일했다.
4. 공고문은 저작재산권의 참가자 귀속을, 원본 HWP 참가서약서는 주최기관 이양을 요구하는 충돌을 확인했다. 원본을 임의 수정하지 않고 주최기관 확인이 필요한 제출 게이트로 남겼다.
5. 공고문은 추첨번호 7개에 ○ 표시를, 원본 HWP는 빨간색 폰트를 요구해 두 방식의 동시 적용 여부를 확인 항목으로 남겼다.
6. 필수 7개 실제 파일·페이지 수·DRAFT·비밀값·확인서약을 검사하고 모두 통과할 때만 50MB 미만 ZIP을 만드는 사전검사기를 추가했다.
7. 제출 사전검사 5개를 포함한 전체 자동 테스트 37개가 통과했다.
